{
  "name": "dhis2-fhir-immunization-bridge",
  "version": "1.0.0",
  "description": "A narrow, real DHIS2 <-> FHIR interoperability tool: syncs FHIR Immunization resources into a DHIS2 Tracker program. Not a DHIS2 App -- a standalone Node CLI, the same architectural shape as real interoperability middleware (see README).",
  "license": "MIT",
  "private": true,
  "main": "dist/cli.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "sync": "ts-node src/cli.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
