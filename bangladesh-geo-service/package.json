{
  "name": "bangladesh-geo-service",
  "version": "1.0.0",
  "description": "A standalone geo lookup + DHIS2 crosswalk API for Bangladesh's Division/District/Upazila/Union administrative geography. Not a DHIS2 app -- shared infrastructure the rest of onehealth-platform's tools can depend on instead of each re-deriving admin geography from shapefiles.",
  "license": "MIT",
  "private": true,
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "build-data": "ts-node scripts/build-data.ts",
    "build-crosswalk": "ts-node scripts/build-crosswalk.ts",
    "start": "ts-node src/server.ts"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.14.0",
    "exceljs": "^4.4.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.4"
  }
}
